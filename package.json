{
  "name": "an-archive-of-your-own",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env server/server.js",
    "test": "AO3_MIN_INTERVAL_MS=0 node --test server/"
  }
}
